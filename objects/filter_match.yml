name: match
type: filter
description: Match a regular expression to the headers and the message itself.
options:
  - match:
      type: string
      description: The regexp.
      required: yes
  - value:
      type: string
      description: The macro to limit the scope of the match to a specific part of the message. 

