name: set
type: rewrite
description: Set a field of the message to a specific value.
options:
  - set:
      type: string
      description: The string to include in the message.
      required: yes
  - value:
      type: string
      description: The field of the message where it should be included.
      required: yes

